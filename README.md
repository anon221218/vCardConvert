# vCardConvert
Python script to parse and convert vCard files (specifically Apple Contacts export format) to CSV or JSON files. It was created for the purpose of making Apple Contacts backups (via vCard file exports) easier to read.
